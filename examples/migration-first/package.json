{
  "name": "ormit-example-migration-first",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "migrations:add": "ormit migrations add",
    "migrations:list": "ormit migrations list",
    "migrations:remove": "ormit migrations remove",
    "migrations:repair": "ormit migrations repair",
    "migrations:has-pending-changes": "ormit migrations has-pending-changes",
    "db:update": "ormit database update",
    "db:down": "ormit database update --down",
    "db:script": "ormit script",
    "seed": "tsx src/seed.ts",
    "reset": "rm -rf data migrations model.snapshot.json"
  },
  "dependencies": {
    "@ormit/cli": "workspace:*",
    "@ormit/core": "workspace:*",
    "@ormit/migrations": "workspace:*",
    "@ormit/sqlite": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
